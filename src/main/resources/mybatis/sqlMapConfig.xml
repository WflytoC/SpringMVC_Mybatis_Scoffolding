<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- 全局setting配置 -->

        <!-- 配置别名 -->
        <typeAliases>
            <package name="po"/>
        </typeAliases>

        <!-- 使用spring和mybatis的整合包进行mapper扫描
                必须遵循：mapper.xml和mapper.java文件同名且在同一目录-->

</configuration>















